spring.application.name=jobboard-backend
server.port=8081

# Database Settings
spring.datasource.url=jdbc:mysql://localhost:3306/job_board_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
# Update this password to your MySQL root password if different
spring.datasource.password= Pranay@123

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
